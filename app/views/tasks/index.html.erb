<div class=wrapper>
  <div class="title">
    <h1>タスク一覧</h1>
  </div>
  <div class="task-index">
    <ul>
      <% @tasks.each do |task| %>
        <li><%= link_to task.content, task %></li>
      <% end %>
    </ul>
    <%= link_to '新規タスクの登録', new_task_path %>
  </div>
</div>
